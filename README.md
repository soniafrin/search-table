This project is a simple React + Vite app that lets users search random order numbers inside a table. A small React-based table search tool that filters random order numbers inside a dataset.

Tech Stack:
  React
  Vite
  JavaScript
  TailwindCSS

Features:
  Search any order number in the table
  Fast filtering using React state
  Clean, minimal UI
  Built with Vite for quick development
